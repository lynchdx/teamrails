<%= form_for(assitance) do |f| %>
  <% if assitance.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(assitance.errors.count, "error") %> prohibited this assitance from being saved:</h2>

      <ul>
      <% assitance.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :assitance_present %>
    <%= f.number_field :assitance_present %>
  </div>

  <div class="field">
    <%= f.label :student_id %>
    <%= f.text_field :student_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
