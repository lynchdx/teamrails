<%= form_for(section) do |f| %>
  <% if section.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(section.errors.count, "error") %> prohibited this section from being saved:</h2>

      <ul>
      <% section.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :section_name %>
    <%= f.text_field :section_name %>
  </div>

  <div class="field">
    <%= f.label :teacher_id %>
    <%= f.text_field :teacher_id %>
  </div>

  <div class="field">
    <%= f.label :day_id %>
    <%= f.text_field :day_id %>
  </div>

  <div class="field">
    <%= f.label :subject_id %>
    <%= f.text_field :subject_id %>
  </div>

  <div class="field">
    <%= f.label :assitance_id %>
    <%= f.text_field :assitance_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
